# 📦 Система управления складом

**Система управления складом** — это веб-приложение для учёта ресурсов, единиц измерения и поступлений на склад.
Проект создан как pet-project для демонстрации навыков работы с **ASP.NET Core MVC**, **Entity Framework Core**, **Clean Architecture** и паттернами проектирования.
Проект находится в активной разработке и будет постепенно расширяться, включая новые функции и улучшения архитектуры.

---

## 🛠️ Стек технологий

* **.NET 8**
* **ASP.NET Core MVC**
* **Entity Framework Core 9.0.8**
* **PostgreSQL** (Neon) / **SQL Server** (LocalDB)
* **Bootstrap 5**
* **C#**
* **AutoMapper 15.0.1**
* **MediatR 12.4.1** (CQRS паттерн)
* **EPPlus 7.0.0** (работа с Excel)
* **Swagger/OpenAPI** (документация API)
* **Dependency Injection**

---

## 🏛 Архитектура и паттерны

Проект построен по принципам **Clean Architecture** с разделением на уровни:

* **Domain** — доменные сущности
* **Application** — DTO, интерфейсы и бизнес-логика
* **Infrastructure** — реализация репозиториев и доступ к данным
* **WebUI** — пользовательский интерфейс (MVC)

Использованы паттерны:

* **Repository** — абстракция доступа к данным
* **Service** — бизнес-логика приложения
* **CQRS** (MediatR) — разделение команд и запросов
* **Dependency Injection** — управление зависимостями
* **AutoMapping** (AutoMapper) — маппинг между DTO и сущностями
* **Background Service** — фоновые задачи (группировка продуктов)

---

## 🚀 Функционал

* Управление **ресурсами** склада:

  * Добавление, редактирование, удаление
  * Запрет на добавление ресурса с уже существующим названием
  * Запрет на удаление ресурса, если он используется в поступлениях
* Управление **единицами измерения**:

  * Добавление, редактирование, удаление
  * Запрет на добавление единицы измерения с уже существующим названием
  * Запрет на удаление единицы измерения, если она используется в поступлениях
* Управление **поступлениями**:

  * Создание поступления с возможностью добавления нескольких позиций
  * Выбор ресурса и единицы измерения для каждой позиции
  * Неактивные ресурсы и единицы измерения не отображаются при выборе, если они не были ранее выбраны в документе
  * Запрет на создание поступления с уже существующим номером
  * Возможность создания пустого документа поступления
  * **Фильтрация**:

    * По датам (периоду)
    * По номерам документов (с выбором нескольких номеров)
    * По ресурсам (с выбором нескольких ресурсов)
    * По единицам измерения (с выбором нескольких единиц)
    * Фильтрация по номерам документов, ресурсам и единицам измерения не зависит от выбранного периода
    * Выполняется на серверной стороне
* Управление **продуктами**:
  * Импорт продуктов из Excel файлов (.xlsx)
  * Автоматическая группировка продуктов (фоновый сервис)
  * Просмотр необработанных продуктов
* Управление **группами продуктов**:
  * Автоматическое создание групп на основе схожести продуктов
  * Просмотр групп и элементов групп
* Удобный и современный интерфейс на **Bootstrap 5**
* Валидация форм на сервере
* **RESTful API** с документацией Swagger

---

## 📋 Требования

Перед запуском проекта убедитесь, что установлены:

* **.NET 8 SDK** или выше ([скачать](https://dotnet.microsoft.com/download/dotnet/8.0))
* **PostgreSQL** (если используете локальную БД) или доступ к **Neon PostgreSQL**
* **SQL Server LocalDB** (опционально, для использования SQL Server)
* **Git** (для клонирования репозитория)
* **IDE**: Visual Studio 2022, Rider, или VS Code с расширением C#

---

## 🚀 Установка и запуск проекта

### 1. Клонирование репозитория

```bash
git clone <repository-url>
cd WarehouseSolution
```

### 2. Настройка базы данных

Проект поддерживает два варианта подключения к базе данных:

#### Вариант A: PostgreSQL (Neon) — используется по умолчанию

1. В файле `Warehouse.WebUI/appsettings.json` уже настроена строка подключения к Neon PostgreSQL
2. Убедитесь, что строка подключения актуальна и доступна

#### Вариант B: SQL Server LocalDB

1. Откройте файл `Warehouse.Infrastructure/DependencyInjection.cs`
2. Раскомментируйте строки для SQL Server и закомментируйте PostgreSQL:
   ```csharp
   // Подключение к базе данных SQL Server
   services.AddDbContext<WarehouseDbContext>(options =>
       options.UseSqlServer(configuration.GetConnectionString("Local")));

   // Подключение к базе данных PostgreSQL
   //services.AddDbContext<WarehouseDbContext>(options =>
   //        options.UseNpgsql(configuration.GetConnectionString("Neon")));
   ```
3. Обновите строку подключения в `appsettings.json` при необходимости

### 3. Применение миграций

При первом запуске необходимо применить миграции базы данных:

```bash
# Перейдите в папку проекта WebUI
cd Warehouse.WebUI

# Примените миграции
dotnet ef database update --project ../Warehouse.Infrastructure
```

**Примечание**: Если команда `dotnet ef` не найдена, установите инструменты EF Core:
```bash
dotnet tool install --global dotnet-ef
```

### 4. Восстановление зависимостей

```bash
# В корневой папке решения
dotnet restore
```

### 5. Запуск проекта

#### Способ 1: Через командную строку

```bash
cd Warehouse.WebUI
dotnet run
```

Приложение будет доступно по адресам:
* HTTP: `http://localhost:5134`
* HTTPS: `https://localhost:7103`

#### Способ 2: Через Visual Studio

1. Откройте файл `WarehouseSolution.sln` в Visual Studio
2. Установите проект `Warehouse.WebUI` как стартовый
3. Нажмите `F5` или выберите **Debug > Start Debugging**

#### Способ 3: Через Rider

1. Откройте решение `WarehouseSolution.sln`
2. Выберите проект `Warehouse.WebUI` как стартовый
3. Нажмите `Shift + F10` или кнопку **Run**

### 6. Доступ к приложению

После запуска откройте браузер и перейдите по адресу:
* **Веб-интерфейс**: `https://localhost:7103` или `http://localhost:5134`
* **Swagger API**: `https://localhost:7103/swagger` (только в режиме Development)

---

## 🧪 Тестирование проекта

### Тестирование через веб-интерфейс

1. **Управление ресурсами** (`/Resource`):
   * Создайте несколько ресурсов (например: "Мука", "Сахар", "Масло")
   * Попробуйте создать ресурс с уже существующим названием — должна появиться ошибка
   * Попробуйте удалить ресурс, который используется в поступлении — должна появиться ошибка

2. **Управление единицами измерения** (`/Unit`):
   * Создайте единицы измерения (например: "кг", "л", "шт")
   * Проверьте валидацию уникальности названий
   * Проверьте защиту от удаления используемых единиц

3. **Управление поступлениями** (`/Receipt`):
   * Создайте новое поступление с несколькими позициями
   * Проверьте фильтрацию по датам, номерам, ресурсам и единицам измерения
   * Создайте поступление с уже существующим номером — должна появиться ошибка
   * Отредактируйте существующее поступление
   * Удалите поступление

4. **Управление продуктами** (`/Products`):
   * Загрузите Excel файл с продуктами через API
   * Проверьте автоматическую группировку продуктов (работает в фоне)

### Тестирование через Swagger API

1. Откройте Swagger UI: `https://localhost:7103/swagger`
2. Протестируйте доступные эндпоинты:
   * `POST /api/Products/upload` — загрузка Excel файла
   * `GET /api/Products/unprocessed` — получение необработанных продуктов

### Тестирование API через curl или Postman

#### Загрузка Excel файла с продуктами

```bash
curl -X POST "https://localhost:7103/api/Products/upload" \
  -H "accept: */*" \
  -H "Content-Type: multipart/form-data" \
  -F "file=@products.xlsx"
```

#### Получение необработанных продуктов

```bash
curl -X GET "https://localhost:7103/api/Products/unprocessed" \
  -H "accept: text/plain"
```

### Проверка работы фонового сервиса

1. Загрузите продукты через API
2. Подождите несколько секунд (фоновый сервис обрабатывает продукты периодически)
3. Проверьте группы продуктов через веб-интерфейс

---

## 📁 Структура проекта

```
WarehouseSolution/
├── Warehouse.Domain/              # Доменный слой
│   ├── Entities/                  # Доменные сущности
│   │   ├── Product.cs
│   │   ├── ProductGroup.cs
│   │   ├── ReceiptDocument.cs
│   │   ├── Resource.cs
│   │   └── Unit.cs
│   └── Interfaces/                # Интерфейсы репозиториев
│
├── Warehouse.Application/         # Слой приложения
│   ├── Commands/                  # Команды (CQRS)
│   │   ├── Products/
│   │   └── ProductGroups/
│   ├── Queries/                   # Запросы (CQRS)
│   │   ├── Products/
│   │   └── ProductGroups/
│   ├── DTOs/                      # Data Transfer Objects
│   ├── Handlers/                  # Обработчики команд и запросов
│   ├── Interfaces/                # Интерфейсы сервисов
│   ├── Mapping/                   # Профили AutoMapper
│   ├── Models/                    # Модели приложения
│   └── Services/                  # Бизнес-логика
│       ├── ProductService.cs
│       ├── ReceiptService.cs
│       └── ProductGroupingBackgroundService.cs
│
├── Warehouse.Infrastructure/      # Слой инфраструктуры
│   ├── Data/                      # DbContext и конфигурация БД
│   ├── Migrations/                 # Миграции Entity Framework
│   └── Repositories/              # Реализация репозиториев
│
└── Warehouse.WebUI/               # Слой представления
    ├── Controllers/               # MVC контроллеры и API контроллеры
    ├── Views/                     # Razor представления
    ├── Models/                    # ViewModels
    └── wwwroot/                   # Статические файлы (CSS, JS)
```

---

## 🔧 Конфигурация

### Строки подключения

Настройка строк подключения находится в файлах:
* `Warehouse.WebUI/appsettings.json` — для production
* `Warehouse.WebUI/appsettings.Development.json` — для development

### Переключение между базами данных

Для переключения между PostgreSQL и SQL Server отредактируйте файл:
`Warehouse.Infrastructure/DependencyInjection.cs`

---

## 📚 API Документация

### Эндпоинты продуктов

#### Загрузка Excel файла
```
POST /api/Products/upload
Content-Type: multipart/form-data
Body: file (Excel файл .xlsx)
```

#### Получение необработанных продуктов
```
GET /api/Products/unprocessed
Response: List<ProductDto>
```

### Swagger UI

В режиме Development доступна интерактивная документация API:
`https://localhost:7103/swagger`

---

## 🐛 Решение проблем

### Ошибка подключения к базе данных

1. Проверьте строку подключения в `appsettings.json`
2. Убедитесь, что база данных доступна
3. Проверьте, применены ли миграции: `dotnet ef database update`

### Ошибка "dotnet ef command not found"

Установите инструменты EF Core:
```bash
dotnet tool install --global dotnet-ef
```

### Порт уже занят

Измените порты в `Warehouse.WebUI/Properties/launchSettings.json` или завершите процесс, использующий порт.

### Ошибки компиляции

1. Выполните `dotnet restore` в корневой папке решения
2. Очистите решение: `dotnet clean`
3. Пересоберите: `dotnet build`

---

## 🔄 Создание новых миграций

При изменении моделей данных создайте новую миграцию:

```bash
cd Warehouse.WebUI
dotnet ef migrations add <MigrationName> --project ../Warehouse.Infrastructure
```

Примените миграцию:
```bash
dotnet ef database update --project ../Warehouse.Infrastructure
```

---

## 📝 Дополнительная информация

### Особенности реализации

* **Автоматическая конвертация DateTime в UTC** — все даты сохраняются в UTC
* **Уникальные индексы** — на названия ресурсов, единиц измерения и номера документов
* **Мягкое удаление** — ресурсы и единицы измерения помечаются как неактивные
* **Фоновый сервис** — автоматическая группировка продуктов работает в фоне
* **Валидация на сервере** — все формы валидируются на сервере перед сохранением

### Расширение функционала

Проект спроектирован для легкого расширения:
* Добавление новых сущностей через Domain слой
* Создание новых сервисов в Application слое
* Реализация новых репозиториев в Infrastructure слое
* Добавление новых контроллеров и представлений в WebUI слое

---

## 👨‍💻 Автор

**Agatay Kulkaiyr** — .NET разработчик

---
